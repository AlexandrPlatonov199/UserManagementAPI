from app.common.api.settings import BaseAPISettings


class Settings(BaseAPISettings):
    """Settings class for the API.

    This class is used to represent the settings required by the API, including the settings inherited from the
    `BaseAPISettings` class.
    """